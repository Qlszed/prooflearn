"use client";
import SetupForm from "@/components/SetupForm";
import LanguageSelector from "@/components/LanguageSelector";
import { t, useLanguage } from "@/lib/i18n";
export default function SetupPage() { const language = useLanguage(); const title = t(language, "setupTitle").split("|"); const margin = t(language, "marginStructure").split("|"); const sub = t(language, "marginSub").split("|"); return <main className="shell inner-page"><header className="topbar"><a className="brand" href="/">ProofLearn<span>.</span></a><span className="step">{t(language, "stage")} </span><LanguageSelector /></header><div className="setup-layout"><div className="form-wrap"><p className="eyebrow">{t(language, "setupEyebrow")}</p><h1>{title[0]}<br /><em>{title[1]}</em></h1><p className="intro">{t(language, "setupIntro")}</p><SetupForm /></div><aside className="margin-note"><span className="note-number">01</span><div className="note-rule" /><p>{margin[0]}<br />{margin[1]}</p><small>{sub[0]}<br />{sub[1]}</small></aside></div></main>; }
